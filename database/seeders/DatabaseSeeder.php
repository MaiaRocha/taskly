<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Intentionally empty — demo data lives in DemoSeeder, run explicitly via
     * `php artisan db:seed --class=DemoSeeder --force`. Production must be
     * able to boot with zero seeded data via `migrate --force` alone.
     */
    public function run(): void
    {
        //
    }
}
